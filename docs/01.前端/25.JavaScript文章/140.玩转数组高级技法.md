# 玩转数组高级技法，成为JS高手

## 1.批量制造数据

一、创建新数组使用 for 循环批量 push 数据

```
function createData() {
  const data = [];
  for (let i = 0; i < 1000; i++) {
    data.push({
      name: `name${i + 1}`,
    });
  }
  return data;
}

const data = createData();
console.log(data);

```