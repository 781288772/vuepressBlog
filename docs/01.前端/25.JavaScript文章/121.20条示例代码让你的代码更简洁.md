1.通过条件判断给变量赋值布尔值的正确姿势

```javascript
// bad
if (a === 'a') {
    b = true
} else {
    b = false
}

// good
b = a === 'a'
```

2.在if中判断数组长度不为零的正确姿势

```javascript
if (arr.length !== 0) {
    // todo
}

// good
if (arr.length) {
    // todo
}

```

